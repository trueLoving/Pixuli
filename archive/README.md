# Archive

本目录存放已从主构建路径移出的历史代码，不参与 `pnpm` workspace 与日常开发。

| 目录         | 说明                                       |
| ------------ | ------------------------------------------ |
| `wasm/`      | 原 `packages/wasm`（Rust / NAPI 图片处理） |
| `benchmark/` | 原 `benchmark/`（WASM 性能基准测试）       |

如需恢复或参考实现，请在本目录内单独构建；主应用已改用浏览器 Canvas 等方案处理图片。
