# Server Port
PORT=6000

# Database
# MySQL 数据库连接字符串
# 格式: mysql://用户名:密码@主机:端口/数据库名
# 示例: mysql://root:password@localhost:3306/pixuli
DATABASE_URL="mysql://root:password@localhost:3306/pixuli"

# Storage Configuration
# 存储类型配置（支持多个，用逗号分隔）：local, minio
# 示例: STORAGE_TYPES=local,minio  (同时使用本地和MinIO)
# 示例: STORAGE_TYPES=local        (仅使用本地存储)
# 示例: STORAGE_TYPES=minio        (仅使用MinIO)
STORAGE_TYPES=local

# Local Storage Configuration
# 本地存储目录（相对于项目根目录）
STORAGE_LOCAL_DIR=uploads/images

# MinIO Configuration
# MinIO 服务端点（主机:端口）
MINIO_ENDPOINT=localhost:9000
# MinIO 访问密钥
MINIO_ACCESS_KEY=minioadmin
# MinIO 秘密密钥
MINIO_SECRET_KEY=minioadmin
# MinIO 存储桶名称
MINIO_BUCKET=pixuli-images
# 是否使用 SSL（true/false）
MINIO_USE_SSL=false
# MinIO 区域（可选）
MINIO_REGION=us-east-1

# Authentication Configuration
# API Key 认证配置
# 方式 1: 环境变量 API Key（简单模式）
# 设置此值后，所有 API 请求都需要提供此 API Key
# 示例: API_KEY=your-secret-api-key-here
API_KEY=

# 方式 2: 数据库 API Key（完整模式）
# 设置为 true 以启用数据库存储的 API Key 管理
# 需要先运行数据库迁移: pnpm prisma migrate dev
# 示例: ENABLE_DB_API_KEYS=true
ENABLE_DB_API_KEYS=false
