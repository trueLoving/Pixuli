# DECISIONS

> **产品与架构决策记录** · 更新：2026-08-25  
> 进度见 [PLANS.md](./PLANS.md)；2.0 能力对照见 [2.0.md](./2.0.md)。

本文记录「为什么这样定」，不是功能清单，也不是 Issue 进度表。

---

## D1 · 为什么要从 1.0 走向 2.0

### 1.0 用下来的问题

1.0 /
1.x 本质是**远端图床客户端**：配好 GitHub/Gitee 仓库，浏览与上传大多直接贴着远端 API。自己长期用会碰到：

- **网与远端绑死**：断网或 API 抖动时，本地几乎没有「完整可用的库」；列表体验跟着远端走。
- **心智是「仓库」不是「我的资料」**：加源、Token、路径优先于「我电脑上的这批文件」。
- **能力边界偏窄**：以图片为主；PDF、视频等难进同一套管理叙事。
- **工程与交付偏碎**：独立 RN
  Mobile、曾探索 Server/WASM 等，维护面大，和「个人天天用」的投入不成比。
- **删改与远端纠缠**：库内操作容易让人以为已经同步到云，实际状态不清晰。

这些不是单点 bug，而是**产品主轴选错了**：主轴在远端，本地只是窗口。

### 需求扩大（个人使用）

作者自己用的过程中，需求从「博客图床」扩大到：

> 希望借助一些**开源的云服务 / 存储服务**（GitHub、Gitee，以及后续可能的 OneDrive、对象存储等），把**自己的资源有效管理起来**——图、文档、视频等可以在同一工作区里找得到、理得顺；云是备份、同步与对外发布的通道，而不是唯一真相。

因此需要：

- **本地优先（Local-first）**：本机工作区是 SSOT；随时可浏览、整理、查询；同步是显式动作。
- **连接多云，但不自建官方 Server**：用现成托管与开源生态，客户端插件化接入。
- **资源库而不仅是图床**：类型可扩展；文件夹与元数据在本地可改，不自动强推远端。
- **三端一份应用**：Web / Desktop / Mobile 同一套
  `app`，降低「自己维护三套产品」的成本。

### 决策结论

| 决策        | 选择                                               |
| ----------- | -------------------------------------------------- |
| 产品主轴    | **本地工作区**；远端 = 可选同步 + 发布             |
| 云的角色    | 借开源/现成存储服务管资源，**非**自建中心化后端    |
| 官方 Server | **不做**一等公民                                   |
| 库内增删改  | **仅本地**；与同步分离                             |
| 工程形态    | 单仓 `app` + `@pixuli/core` + `@pixuli/provider-*` |

1.0→2.0 是产品重定位，不是单纯的大版本号 bump。能力对照见 [2.0.md](./2.0.md)。

---

## D2 · 相关决策（摘要）

| ID   | 决策                          | 要点                                              |
| ---- | ----------------------------- | ------------------------------------------------- |
| D2.1 | 无官方 NestJS Server          | 降低运维；存储走 Provider                         |
| D2.2 | core / provider 禁止依赖 UI   | 插件与契约可独立演进                              |
| D2.3 | UI 内联 `app/src/ui`          | 唯一消费者，少一层包税                            |
| D2.4 | 展示裁剪（幻灯片/时间线等）   | 聚焦资源管理主路径                                |
| D2.5 | Mobile = Capacitor，非独立 RN | 与 Web/Desktop 同 UI                              |
| D2.6 | 默认 TypeScript               | 业务与 provider 一律 `.ts`/`.tsx`；工具链例外另记 |

新增重大取舍时，在本文追加 `D3…`，并在 PR / Issue 里引用编号。
