{
  "name": "pixuli-benchmark",
  "version": "1.0.0",
  "description": "WASM vs JavaScript 图片压缩性能对比测试",
  "type": "module",
  "scripts": {
    "benchmark": "pnpm run benchmark:simple && pnpm run benchmark:detailed",
    "benchmark:simple": "tsx src/simple-compression-bench.ts",
    "benchmark:detailed": "tsx src/detailed-bench.ts"
  },
  "dependencies": {
    "pixuli-wasm": "workspace:*",
    "tinybench": "^2.5.0",
    "browser-image-compression": "^2.0.2"
  },
  "devDependencies": {
    "tsx": "^4.0.0",
    "@types/node": "^22.0.0"
  }
}
