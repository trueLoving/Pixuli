---
description: Electron 桌面：main/preload 进程边界与 Vite 配置
globs: apps/pixuli/electron/**,apps/pixuli/vite.config.ts
alwaysApply: false
---

# Electron & Desktop

## 进程边界

- **Main**：`electron/main/` — IPC、工作区、AI 等服务
- **Preload**：`electron/preload/` — `contextBridge` 暴露 `workspaceAPI` 等
- **Renderer**：普通 React；**禁止** `node:http`、`fs` 等 Node 内置模块

## Vite

`vite.config.ts`：Renderer `resolve.conditions` 含 `development`；SSR 走 `dist`（REF-416）。

## 桌面开发

- `pnpm dev:desktop` — 会先 `build:packages`；改 main/preload 后需重启
- 与 Web 共用 `apps/pixuli/src`；平台分支用 `__IS_WEB__` 或 `getAppPlatform()`
